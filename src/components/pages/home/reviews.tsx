import { api } from "@/utils/api";
export default function HomeReviews() {
  const { data } = api.reviews.approved.useQuery(); if (!data?.length) return null;
  return <section className="border-t border-white/10 px-4 py-20 lg:px-10"><div className="mx-auto max-w-7xl"><div className="flex items-end justify-between gap-6"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-[#B9FF00]">Customer signal</p><h2 className="mt-3 text-4xl font-black md:text-5xl">Trusted in the booth</h2></div><p className="hidden max-w-sm text-right text-sm text-zinc-500 md:block">Ten approved customer reviews, refreshed in a different order.</p></div><div className="mt-9 grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">{data.map((r,index) => <article key={r.id} className="flex min-h-56 flex-col rounded-2xl border border-white/10 bg-gradient-to-b from-white/[.045] to-transparent p-5 transition hover:-translate-y-1 hover:border-[#B9FF00]/25"><div className="flex items-center justify-between"><p className="text-sm text-[#B9FF00]">{"★".repeat(r.rating)}</p><span className="text-[10px] text-zinc-700">{String(index + 1).padStart(2,"0")}</span></div>{r.title && <h3 className="mt-4 line-clamp-1 font-bold">{r.title}</h3>}<p className="mt-3 line-clamp-5 text-xs leading-5 text-zinc-400">{r.message}</p><p className="mt-auto pt-5 text-xs font-bold text-white">{r.user.name ?? "Verified customer"}</p></article>)}</div></div></section>;
}
